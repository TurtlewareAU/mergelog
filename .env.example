# Comma-separated agent:token credentials. Use long random values outside local testing.
MCP_TOKENS=codex:replace-with-a-long-random-token,claude:replace-with-another-long-token,opencode:replace-with-a-third-long-token
JOURNAL_PORT=3000
# Add the LAN hostname and/or IP used in your browser before deployment.
ALLOWED_HOSTS=localhost,127.0.0.1,journal,mergelog.local,10.3.40.24
